#include <kernel/fs.h>

void init_fs_internal() {
	
}

FILE ffind (const char *filename) {
	if ((filename[0]=='A'||filename[0]=='a')&&filename[1]==":") {
		
	}
}

FILE fopen_internal(const char *filename, _Bool mode) {
	
}