{
    "version": "0.2.1",
    "configurations": [
        {
            "name": "Run Make Run",
            "type": "node-terminal",
            "request": "launch",
            "command": "make run",
            "cwd": "${workspaceFolder}"
        }
    ]
}